// swift-tools-version:5.1

import PackageDescription

let package = Package(
    name: "Archive",
    products: [
        .library(
            name: "Archive",
            targets: ["Archive"]),
    ],
    dependencies: [
    ],
    targets: [
        .target(
            name: "Archive",
            dependencies: []),
    ]
)
